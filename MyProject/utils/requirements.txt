# 主框架版本
torch==2.1.2            # 与CUDA 12.1配套
transformers==4.52.4           # 2025年最新版本
datasets==3.6.0                # 最新兼容性（注意比官方版本旧）
accelerate==1.7.0              # 支持大模型分布式和量化推理
peft==0.15.2             # 支持 AdaLoRA 开发版
trl==0.18.1                    # 支持 DPOTrainer 和最新策略微调

# LoRA量化优化器
bitsandbytes==0.42.0           # 支持 4bit/8bit 训练

# 科学计算与兼容性保障
numpy==1.26.4                  # <2.0，防止兼容性报错
scipy==1.11.4                  # 与 transformers/scipy.optimize 配套
tqdm==4.67.1                  # 训练进度显示

# HF生态需要
sentencepiece>=0.1.99
protobuf>=3.20.3


tensorboard